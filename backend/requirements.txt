# Pins chosen so the backend installs cleanly on Python 3.14 (cp314 wheels).
# Notably asyncpg>=0.31, pydantic>=2.13, sqlalchemy>=2.0.50, greenlet>=3.5 are
# the first releases shipping prebuilt cp314 wheels (earlier pins forced a
# from-source build that needs Rust/C toolchains).

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.50
asyncpg==0.31.0
greenlet==3.5.1            # required by SQLAlchemy's async engine
alembic==1.14.0
pgvector==0.3.5

# Supabase client
supabase==2.10.0

# AI
anthropic==0.40.0
openai==1.57.0

# Policy ingestion (PDF parsing + tokenization)
pypdf==5.1.0
tiktoken==0.13.0          # 0.8.0 has no cp314 wheel; 0.13.0 ships prebuilt wheels

# Validation & settings
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.2.0    # required by pydantic EmailStr (used in user schema)
python-multipart==0.0.20

# Auth / security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP client
# Capped <0.28 because supabase 2.10.0 requires httpx<0.28 (openai/anthropic
# accept this range too). The original httpx==0.28.1 conflicted with supabase.
httpx==0.27.2

# Dev / testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
ruff==0.8.3
